# คู่มือการเปรียบเทียบข้อมูล กกต. vs Vote62.com

## 🎯 ภาพรวม

ตามที่ภาคประชาชนรวบรวมข้อมูลผ่าน **Vote62.com** (จัดทำโดย iLaw, Rocket Media Lab และ Opendream) เพื่อเป็นพยานหลักฐานอิสระควบคู่กับข้อมูลทางการจาก กกต. ระบบนี้จะช่วยเปรียบเทียบข้อมูลจากทั้งสองแหล่งอย่างเป็นระบบ

### ข้อมูลปัจจุบัน (12 กุมภาพันธ์ 2026)
- 📸 Vote62 มีภาพถ่ายแล้วจาก **31,200+ หน่วย** (เกือบ 1/3 ของทั้งหมด)
- 🎯 เป้าหมาย: เปรียบเทียบทุกหน่วยที่มีข้อมูลครบทั้งสองแหล่ง
- ⚠️ ความสำคัญ: ความแตกต่างแม้เล็กน้อยอาจบ่งบอกถึงการเปลี่ยนแปลงข้อมูลในระดับกลาง

---

## 📊 แหล่งข้อมูลทั้งสอง

### 1. กกต. (Electoral Commission of Thailand)
**ลักษณะ:** ข้อมูลทางการที่ผ่านกระบวนการ workflow ดังนี้
```
หน่วยเลือกตั้ง (กปน.) 
    ↓
ภาพถ่าย + กรอกข้อมูล (เจ้าหน้าที่เขต)
    ↓
⚠️ ตรวจสอบโดยเจ้าหน้าที่อำเภอ (จุดที่อาจเปลี่ยนแปลงข้อมูล)
    ↓
ส่งเข้าส่วนกลาง
    ↓
Dashboard สาธารณะ
```

**API:** `https://static-ectreport69.ect.go.th/data/data/`

**ข้อมูลที่ได้:**
- ผลคะแนนแยกตามพรรค/ผู้สมัคร
- จำนวนบัตรเสีย
- จำนวนผู้มาใช้สิทธิ
- Timestamp ของการอัพเดท

### 2. Vote62.com (Citizen-sourced)
**ลักษณะ:** ข้อมูลจากภาคประชาชน

**แหล่งข้อมูล:**
1. ภาพถ่ายจาก กปน. (กรรมการประจำหน่วยเลือกตั้ง)
2. ภาพถ่ายจากอาสาสมัคร
3. การกรอกข้อมูลโดยอาสา (นับจากภาพ)

**ข้อดี:**
- ✅ ไม่ผ่านระบบกลางของ กกต.
- ✅ มีภาพถ่ายต้นฉบับ (แบบ 5/11 และ 5/18)
- ✅ ไม่สามารถถูกแก้ไขโดย กกต.
- ✅ ตรวจสอบได้โดยประชาชน

**ข้อจำกัด:**
- ⚠️ อาจไม่ครบทุกหน่วย (ขึ้นกับการส่งภาพจากอาสา)
- ⚠️ อาจมีข้อผิดพลาดจากการกรอกข้อมูล (แก้ไขได้โดยการกรอกซ้ำ)

---

## 🔍 วิธีการเปรียบเทียบ

### ระดับ 1: เปรียบเทียบแบบ Manual (ทำเองได้)

#### ขั้นตอน:
1. **เลือกหน่วยเลือกตั้งที่ต้องการตรวจสอบ**
   - หน่วยในเขตของคุณ
   - หน่วยที่มีข่าวลือเรื่องการทุจริต
   - สุ่มตรวจสอบ

2. **ดึงข้อมูลจาก กกต.**
   - เข้า https://www.ect.go.th/ect_th/th/election-2026
   - ค้นหาเขตและหน่วยที่ต้องการ
   - บันทึกคะแนนแต่ละพรรค/ผู้สมัคร

3. **ดึงข้อมูลจาก Vote62**
   - เข้า https://vote62.com
   - ค้นหาหน่วยเดียวกัน
   - ดูภาพถ่ายและข้อมูลที่กรอก

4. **เปรียบเทียบ**
   - ตรวจสอบว่าคะแนนแต่ละพรรค/ผู้สมัครตรงกันหรือไม่
   - **ควรตรงกันเป๊ะ 100%**
   - หากแตกต่างแม้ 1 คะแนน → น่าสงสัย

5. **บันทึกหลักฐาน**
   - Screenshot จากทั้งสองแหล่ง
   - บันทึกรหัสหน่วย วันเวลา
   - ระบุส่วนที่แตกต่าง

### ระดับ 2: ใช้ระบบอัตโนมัติ (Python Script)

```python
from vote62_comparator import Vote62Comparator

# สร้าง instance
comparator = Vote62Comparator()

# เปรียบเทียบหน่วยเดียว
result = comparator.compare_unit_results(
    unit_id="001001",
    constituency="กรุงเทพมหานคร เขต 1"
)

# เปรียบเทียบหลายหน่วยพร้อมกัน
unit_ids = ["001001", "001002", "001003", ...]
constituencies = {
    "001001": "กรุงเทพมหานคร เขต 1",
    "001002": "กรุงเทพมหานคร เขต 1",
    ...
}

df = comparator.batch_compare(unit_ids, constituencies)

# สร้างรายงาน
comparator.print_summary()
comparator.export_to_csv("comparison_results.csv")
```

### ระดับ 3: ใช้ Web Dashboard

1. เปิดไฟล์ `vote62_dashboard.html`
2. กรอกรหัสหน่วยเลือกตั้ง
3. กดปุ่ม "เปรียบเทียบข้อมูล"
4. ดูผลการเปรียบเทียบแบบ real-time

---

## 📋 รูปแบบความแตกต่างที่พบได้

### 1. เหมือนกันเป๊ะ (IDENTICAL) ✅
```
กกต.:   พรรค A = 1,500 | พรรค B = 1,200 | พรรค C = 800
Vote62: พรรค A = 1,500 | พรรค B = 1,200 | พรรค C = 800
ผล: ✅ ข้อมูลถูกต้อง ไม่มีการเปลี่ยนแปลง
```

### 2. แตกต่างเล็กน้อย (MINOR) ⚠️
```
กกต.:   พรรค A = 1,503 | พรรค B = 1,200 | พรรค C = 800
Vote62: พรรค A = 1,500 | พรรค B = 1,200 | พรรค C = 800
ผล: ต่าง 3 คะแนน - อาจเป็นข้อผิดพลาดในการกรอก
การดำเนินการ: ตรวจสอบภาพถ่ายใน Vote62 อีกครั้ง
```

### 3. แตกต่างมาก (SIGNIFICANT) 🚨
```
กกต.:   พรรค A = 1,530 | พรรค B = 1,200 | พรรค C = 800
Vote62: พรรค A = 1,500 | พรรค B = 1,200 | พรรค C = 800
ผล: ต่าง 30 คะแนน - น่าสงสัย
การดำเนินการ: ตรวจสอบภาพถ่าย + รายงานต่อ iLaw/Vote62
```

### 4. แตกต่างร้ายแรง (CRITICAL) 🔴
```
กกต.:   พรรค A = 1,650 | พรรค B = 1,200 | พรรค C = 800
Vote62: พรรค A = 1,500 | พรรค B = 1,200 | พรรค C = 800
ผล: ต่าง 150 คะแนน - มีการเปลี่ยนแปลงข้อมูลอย่างชัดเจน

หรือ

กกต.:   พรรค A = 1,500 | พรรค B = 1,200 | พรรค C = 800
Vote62: พรรค A = 1,500 | พรรค B = 1,200 | พรรค C = 800 | พรรค D = 300
ผล: พรรค D หายไปจากข้อมูล กกต. 🚨

การดำเนินการ: รายงานด่วน + รวบรวมหลักฐาน + แจ้ง กกต. และสื่อมวลชน
```

---

## 🚨 พบความผิดปกติ - ทำอย่างไร?

### ขั้นตอนที่ 1: ตรวจสอบซ้ำ
1. ✅ ตรวจสอบว่าเปรียบเทียบหน่วยเดียวกันจริง
2. ✅ ดูภาพถ่ายใน Vote62 อีกครั้ง (อาจมีการกรอกผิด)
3. ✅ เปรียบเทียบกับข้อมูลจากแหล่งอื่น (ถ้ามี)

### ขั้นตอนที่ 2: บันทึกหลักฐาน
```
รายละเอียดที่ต้องบันทึก:
- รหัสหน่วยเลือกตั้ง
- ชื่อเขตเลือกตั้ง
- วันเวลาที่ตรวจสอบ
- คะแนนจาก กกต. (Screenshot)
- คะแนนจาก Vote62 (Screenshot + ภาพถ่ายต้นฉบับ)
- ส่วนที่แตกต่างชัดเจน
```

### ขั้นตอนที่ 3: รายงาน

#### ช่องทางการรายงาน:
1. **iLaw (Internet Law Reform Dialogue)**
   - เว็บไซต์: https://ilaw.or.th
   - Facebook: iLaw - ไอลอว์
   - วิธีรายงาน: ส่งข้อมูล + หลักฐานผ่านช่องทางออนไลน์

2. **Vote62.com**
   - เว็บไซต์: https://vote62.com
   - มีฟอร์มรายงานภาพผิดปกติในระบบ
   - ติดต่อผู้ดูแลระบบ

3. **กกต. (Electoral Commission)**
   - สายด่วน: 1-444
   - เว็บไซต์: https://www.ect.go.th
   - ⚠️ หมายเหตุ: อาจไม่ได้รับการตอบสนองหากเป็นการทุจริตระดับระบบ

4. **สื่อมวลชน**
   - แชร์ข้อมูลบนโซเชียลมีเดีย
   - ใช้ hashtag: #นับใหม่ทั้งประเทศ #ระบอบหน้าด้าน
   - ติดต่อสื่อสืบสวนสอบสวน

### ขั้นตอนที่ 4: รวบรวมเป็นคดี
หากพบความผิดปกติหลายหน่วย:
- รวบรวมหลักฐานทั้งหมด
- จัดทำรายงานสรุป
- เตรียมฟ้อง กกต. ในอนาคต (ถ้าจำเป็น)

---

## 💡 Tips สำหรับอาสาสมัคร

### การกรอกคะแนนใน Vote62 ให้ถูกต้อง

1. **ตรวจสอบรูปภาพให้ชัดเจน**
   - ถ้าภาพไม่ชัด → ใช้ปุ่ม "แจ้งรูปผิดปกติ"
   - ดูรูปหลายๆ ภาพที่อัพโหลดจากหน่วยเดียวกัน

2. **นับคะแนนให้ถูกต้อง**
   - ใช้เวลาประมาณ 4-5 นาทีต่อหน่วย
   - เช็คการบวกเลข (คะแนนรวมต้องตรงกับที่ระบุ)
   - หากพบว่าบวกผิด → โน้ตไว้แยก

3. **ตรวจสอบความสอดคล้อง**
   - แบบ 5/11 (ใบขีดคะแนน) vs แบบ 5/18 (รายงานคะแนน)
   - ตัวเลขต้องตรงกัน

---

## 📈 สถิติที่น่าสนใจ

### จำนวนหน่วยเลือกตั้งในประเทศไทย
- **รวมทั้งหมด:** ~95,000 หน่วย
- **มีข้อมูลใน Vote62 (12 ก.พ. 2026):** 31,200+ หน่วย (33%)
- **เป้าหมาย:** ครอบคลุม 50%+ ภายในสัปดาห์หน้า

### อัตราความแตกต่างที่ยอมรับได้
```
0 คะแนน        = ✅ ยอดเยี่ยม (น่าเชื่อถือ 100%)
1-5 คะแนน      = ⚠️  ยอมรับได้ (อาจเป็น human error)
6-20 คะแนน     = 🚨 น่าสงสัย (ควรตรวจสอบ)
21+ คะแนน      = 🔴 ร้ายแรง (มีการเปลี่ยนแปลงข้อมูล)
```

---

## 🛠️ เครื่องมือเพิ่มเติม

### 1. Data Scraper (สำหรับคนเก่ง IT)
```python
# ดาวน์โหลดข้อมูลทั้งหมดจาก กกต.
import requests
import json

def download_all_ect_data():
    base_url = "https://static-ectreport69.ect.go.th/data/data"
    # ... implementation
```

### 2. Automated Comparison
```python
# เปรียบเทียบอัตโนมัติทุกหน่วย
from vote62_comparator import Vote62Comparator
import pandas as pd

def compare_all_units():
    # โหลดรายการหน่วยทั้งหมด
    units_df = pd.read_csv('all_units.csv')
    
    comparator = Vote62Comparator()
    results = comparator.batch_compare(
        units_df['unit_id'].tolist(),
        units_df.set_index('unit_id')['constituency'].to_dict()
    )
    
    # บันทึกผล
    results.to_csv('full_comparison.csv')
```

### 3. Real-time Monitoring
```python
# ติดตามการอัพเดทแบบ real-time
import time

def monitor_updates():
    while True:
        # ตรวจสอบข้อมูลใหม่ทุก 5 นาที
        check_for_updates()
        time.sleep(300)
```

---

## ❓ FAQ

**Q: ทำไมต้องเปรียบเทียบ? ข้อมูล กกต. ไม่น่าเชื่อถือหรือ?**
A: การเปรียบเทียบเป็นหลักการ "check and balance" ในระบอบประชาธิปไตย ข้อมูลจากหลายแหล่งช่วยให้มั่นใจได้ว่าไม่มีการเปลี่ยนแปลงข้อมูลในระดับกลาง

**Q: ถ้าพบความแตกต่าง แปลว่ามีการโกงหรือเปล่า?**
A: ไม่จำเป็น อาจเป็นข้อผิดพลาดในการกรอกข้อมูล Vote62 (ซึ่งแก้ไขได้) แต่ถ้าพบความแตกต่างอย่างเป็นระบบหลายหน่วย → น่าสงสัยมาก

**Q: ข้อมูล Vote62 น่าเชื่อถือแค่ไหน?**
A: Vote62 มีภาพถ่ายต้นฉบับจากหน่วยเลือกตั้ง ซึ่งถือเป็นหลักฐานชั้นต้น ข้อมูลการกรอกอาจมีข้อผิดพลาด แต่สามารถกรอกซ้ำและตรวจสอบจากภาพได้

**Q: ทำไมไม่เปรียบเทียบกับภาพถ่ายจาก กกต. เลย?**
A: กกต. ไม่ได้เปิดเผยภาพถ่ายต้นฉบับ (แบบ 5/18) ให้สาธารณะดาวน์โหลดได้ง่าย ดังนั้นต้องพึ่งข้อมูลที่ภาคประชาชนรวบรวม

---

## 📞 ติดต่อและสนับสนุน

### รายงานปัญหาหรือข้อเสนอแนะ
- GitHub Issues: [repository-url]
- Email: [support-email]
- Facebook Group: [group-url]

### ร่วมพัฒนาระบบ
- Fork และ contribute code
- แชร์ประสบการณ์การใช้งาน
- ช่วยกรอกคะแนนใน Vote62

---

**อัพเดทล่าสุด:** 12 กุมภาพันธ์ 2026  
**เวอร์ชัน:** 2.0 (รองรับ Vote62 Integration)  
**ผู้พัฒนา:** Election Verification System Team

#นับใหม่ทั้งประเทศ #ระบอบหน้าด้าน #Vote62 #iLaw
